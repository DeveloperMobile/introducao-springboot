#DEV enviroment

app:
  message: Este e o arquivo de propriedade para o ${spring.application.name} especifico para ambiente DEV

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:men:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa